# global_costmap:
#   global_frame: map
#   robot_base_frame: base_link
#   update_frequency: 5.0
#   publish_frequency: 2.0
#   width: 50.0
#   height: 50.0
#   static_map: false

       # Adjust resolution for balance between detail and performance
  # width: 1000.0            # Increase width to cover larger area
  # height: 1000.0           # Increase height to cover larger area
  # origin_x: -500.0
  # origin_y: -500.0

global_costmap:
  global_frame: "map"
  robot_base_frame: "base_link"
  update_frequency: 5.0
  publish_frequency: 5.0
  transform_tolerance: 5.0
  width: 100.0
  height: 100.0
  origin_x: -50.0
  origin_y: -50.0
  static_map: true

  plugins:
    - {name: static_layer, type: "costmap_2d::StaticLayer"}
    - {name: obstacle_layer, type: "costmap_2d::ObstacleLayer"}
    - {name: inflation_layer, type: "costmap_2d::InflationLayer"}
